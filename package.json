{
  "name": "pop-cat-volume-visualizer",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "pack": "electron-builder --dir",
    "dist": "electron-builder"
  },
  "devDependencies": {
    "electron": "^16.0.5",
    "electron-builder": "^22.14.5"
  },
  "dependencies": {
    "win-audio": "^2.0.2"
  },
  "build": {
    "appId": "pop-cat-volume-visualizer",
    "extraFiles": [{
      "from": "images",
      "to": "images"
    }],
    "directories": {
      "buildResources": "build"
    }
  }
}